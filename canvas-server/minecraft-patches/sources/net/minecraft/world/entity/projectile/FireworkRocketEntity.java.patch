From 797ef1098eaaa50c9bf6a3ac86ebfa511db756df Mon Sep 17 00:00:00 2001
From: File <noreply+automated@papermc.io>
Date: Sun, 20 Apr 1997 15:37:42 +0200
Subject: [PATCH] canvas File Patches


diff --git a/net/minecraft/world/entity/projectile/FireworkRocketEntity.java b/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
index dafb6c76a4b2d8c0a3fa9405c563e0ab9319584e..bb01509a2c8ae41b53cd10a477510c4661856b12 100644
--- a/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
+++ b/net/minecraft/world/entity/projectile/FireworkRocketEntity.java
@@ -126,6 +126,23 @@ public class FireworkRocketEntity extends Projectile implements ItemSupplier {
         super.inactiveTick();
     }
     // Paper end - EAR 2
+    // Canvas start - MC-261810
+
+    @Override
+    protected void onBelowWorld() {
+        if (
+            io.canvasmc.canvas.GlobalConfiguration.getInstance().vanillaFixes.mc261810 &&
+            this.attachedToEntity != null &&
+            ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor(this.attachedToEntity)
+        ) {
+            // we shouldn't discard the entity if the attached entity is still
+            // in the same tick region and we are below the void, otherwise the
+            // firework gets discarded and the boost stops like 1 tick in
+            return;
+        }
+        super.onBelowWorld();
+    }
+    // Canvas end - MC-261810
 
     @Override
     public void tick() {
@@ -147,7 +164,7 @@ public class FireworkRocketEntity extends Projectile implements ItemSupplier {
 
             if (this.attachedToEntity != null) {
                 Vec3 handAngle;
-                if (this.attachedToEntity.isFallFlying()) {
+                if (this.attachedToEntity.isFallFlying() && (!io.canvasmc.canvas.GlobalConfiguration.getInstance().vanillaFixes.mc119754 || !this.attachedToEntity.isSpectator())) { // Canvas - MC-119754
                     Vec3 lookAngle = this.attachedToEntity.getLookAngle();
                     double power = 1.5;
                     double powerAdd = 0.1;
